diff --git a/td/telegram/ForumTopicManager.cpp b/td/telegram/ForumTopicManager.cpp
index 8542dfb..d05d750 100644
--- a/td/telegram/ForumTopicManager.cpp
+++ b/td/telegram/ForumTopicManager.cpp
@@ -27,6 +27,7 @@
 #include "td/telegram/misc.h"
 #include "td/telegram/NotificationManager.h"
 #include "td/telegram/NotificationSettingsManager.h"
+#include "td/telegram/OptionManager.h"
 #include "td/telegram/Td.h"
 #include "td/telegram/TdDb.h"
 #include "td/telegram/telegram_api.h"
@@ -605,7 +606,9 @@ void ForumTopicManager::read_forum_topic_messages(DialogId dialog_id, ForumTopic
     auto max_message_id = last_read_inbox_message_id.get_prev_server_message_id();
     LOG(INFO) << "Send read topic history request in topic of " << forum_topic_id << " in " << dialog_id << " up to "
               << max_message_id;
-    td_->create_handler<ReadForumTopicQuery>()->send(dialog_id, forum_topic_id, max_message_id);
+    if (!td_->option_manager_->get_option_boolean("x_moex_ghost_read_groups")) {
+      td_->create_handler<ReadForumTopicQuery>()->send(dialog_id, forum_topic_id, max_message_id);
+    }
   }
   if (need_update) {
     on_forum_topic_changed(dialog_id, topic);
diff --git a/td/telegram/MessageQueryManager.cpp b/td/telegram/MessageQueryManager.cpp
index 2b295bd..d668957 100644
--- a/td/telegram/MessageQueryManager.cpp
+++ b/td/telegram/MessageQueryManager.cpp
@@ -35,6 +35,7 @@
 #include "td/telegram/MessageSender.h"
 #include "td/telegram/MessagesInfo.h"
 #include "td/telegram/MessagesManager.h"
+#include "td/telegram/OptionManager.h"
 #include "td/telegram/ReplyMarkup.h"
 #include "td/telegram/RequestedDialogType.h"
 #include "td/telegram/RichMessage.h"
@@ -4143,36 +4144,72 @@ class MessageQueryManager::ReadMessageContentsOnServerLogEvent {
  public:
   DialogId dialog_id_;
   vector<MessageId> message_ids_;
+  bool allow_in_ghost_mode_ = false;
 
   template <class StorerT>
   void store(StorerT &storer) const {
     td::store(dialog_id_, storer);
     td::store(message_ids_, storer);
+    BEGIN_STORE_FLAGS();
+    STORE_FLAG(allow_in_ghost_mode_);
+    END_STORE_FLAGS();
   }
 
   template <class ParserT>
   void parse(ParserT &parser) {
     td::parse(dialog_id_, parser);
     td::parse(message_ids_, parser);
+    if (parser.get_left_len() != 0) {
+      BEGIN_PARSE_FLAGS();
+      PARSE_FLAG(allow_in_ghost_mode_);
+      END_PARSE_FLAGS();
+    }
   }
 };
 
 uint64 MessageQueryManager::save_read_message_contents_on_server_log_event(DialogId dialog_id,
-                                                                           const vector<MessageId> &message_ids) {
-  ReadMessageContentsOnServerLogEvent log_event{dialog_id, message_ids};
+                                                                           const vector<MessageId> &message_ids,
+                                                                           bool allow_in_ghost_mode) {
+  ReadMessageContentsOnServerLogEvent log_event{dialog_id, message_ids, allow_in_ghost_mode};
   return binlog_add(G()->td_db()->get_binlog(), LogEvent::HandlerType::ReadMessageContentsOnServer,
                     get_log_event_storer(log_event));
 }
 
 void MessageQueryManager::read_message_contents_on_server(DialogId dialog_id, vector<MessageId> message_ids,
                                                           uint64 log_event_id, Promise<Unit> &&promise,
-                                                          bool skip_log_event) {
+                                                          bool skip_log_event, bool allow_in_ghost_mode) {
   CHECK(!message_ids.empty());
 
+  bool ghost_read = false;
+  switch (dialog_id.get_type()) {
+    case DialogType::User:
+    case DialogType::SecretChat:
+      ghost_read = td_->option_manager_->get_option_boolean("x_moex_ghost_read_private");
+      break;
+    case DialogType::Chat:
+      ghost_read = td_->option_manager_->get_option_boolean("x_moex_ghost_read_groups");
+      break;
+    case DialogType::Channel:
+      ghost_read = td_->dialog_manager_->is_broadcast_channel(dialog_id)
+                       ? td_->option_manager_->get_option_boolean("x_moex_ghost_read_channels")
+                       : td_->option_manager_->get_option_boolean("x_moex_ghost_read_groups");
+      break;
+    case DialogType::None:
+      break;
+    default:
+      UNREACHABLE();
+  }
+  if (ghost_read && !allow_in_ghost_mode) {
+    if (log_event_id != 0) {
+      binlog_erase(G()->td_db()->get_binlog(), log_event_id);
+    }
+    return promise.set_value(Unit());
+  }
+
   LOG(INFO) << "Read contents of " << message_ids << " in " << dialog_id << " on server";
 
   if (log_event_id == 0 && G()->use_message_database() && !skip_log_event) {
-    log_event_id = save_read_message_contents_on_server_log_event(dialog_id, message_ids);
+    log_event_id = save_read_message_contents_on_server_log_event(dialog_id, message_ids, allow_in_ghost_mode);
   }
 
   auto new_promise = get_erase_log_event_promise(log_event_id, std::move(promise));
@@ -4217,7 +4254,8 @@ void MessageQueryManager::read_message_reactions_on_server(DialogId dialog_id, v
         send_closure(actor_id, &MessageQueryManager::on_read_message_reactions, dialog_id, std::move(message_ids),
                      std::move(result));
       });
-  read_message_contents_on_server(dialog_id, std::move(message_ids), 0, std::move(promise));
+  // Reading reactions doesn't advance the dialog history read position and must remain available in Ghost Mode.
+  read_message_contents_on_server(dialog_id, std::move(message_ids), 0, std::move(promise), false, true);
 }
 
 void MessageQueryManager::on_read_message_reactions(DialogId dialog_id, vector<MessageId> &&message_ids,
@@ -4585,7 +4623,8 @@ void MessageQueryManager::on_binlog_events(vector<BinlogEvent> &&events) {
           break;
         }
 
-        read_message_contents_on_server(dialog_id, std::move(log_event.message_ids_), event.id_, Auto());
+        read_message_contents_on_server(dialog_id, std::move(log_event.message_ids_), event.id_, Auto(), false,
+                                        log_event.allow_in_ghost_mode_);
         break;
       }
       case LogEvent::HandlerType::UnpinAllDialogMessagesOnServer: {
diff --git a/td/telegram/MessageQueryManager.h b/td/telegram/MessageQueryManager.h
index 941f381..8ff2097 100644
--- a/td/telegram/MessageQueryManager.h
+++ b/td/telegram/MessageQueryManager.h
@@ -250,7 +250,8 @@ class MessageQueryManager final : public Actor {
                                             Promise<Unit> &&promise);
 
   void read_message_contents_on_server(DialogId dialog_id, vector<MessageId> message_ids, uint64 log_event_id,
-                                       Promise<Unit> &&promise, bool skip_log_event = false);
+                                       Promise<Unit> &&promise, bool skip_log_event = false,
+                                       bool allow_in_ghost_mode = false);
 
   void read_message_reactions_on_server(DialogId dialog_id, vector<MessageId> message_ids);
 
@@ -380,7 +381,8 @@ class MessageQueryManager final : public Actor {
   static uint64 save_read_all_dialog_poll_votes_on_server_log_event(DialogId dialog_id, ForumTopicId forum_topic_id);
 
   static uint64 save_read_message_contents_on_server_log_event(DialogId dialog_id,
-                                                               const vector<MessageId> &message_ids);
+                                                               const vector<MessageId> &message_ids,
+                                                               bool allow_in_ghost_mode);
 
   static uint64 save_unpin_all_dialog_messages_on_server_log_event(DialogId dialog_id);
 
diff --git a/td/telegram/MessagesManager.cpp b/td/telegram/MessagesManager.cpp
index f144f22..95f4cd4 100644
--- a/td/telegram/MessagesManager.cpp
+++ b/td/telegram/MessagesManager.cpp
@@ -16543,6 +16543,18 @@ Status MessagesManager::close_dialog(DialogId dialog_id) {
   return Status::OK();
 }
 
+static bool consume_moex_ghost_read_once(Td *td, int64 chat_id, int64 message_id) {
+  if (chat_id == 0 || message_id <= 0) {
+    return false;
+  }
+  auto token = td->option_manager_->get_option_string("x_moex_ghost_read_once");
+  if (token != (PSTRING() << chat_id << ':' << message_id)) {
+    return false;
+  }
+  td->option_manager_->set_option_empty("x_moex_ghost_read_once");
+  return true;
+}
+
 Status MessagesManager::view_messages(DialogId dialog_id, vector<MessageId> message_ids, MessageSource source,
                                       bool force_read) {
   CHECK(!td_->auth_manager_->is_bot());
@@ -16819,8 +16831,9 @@ Status MessagesManager::view_messages(DialogId dialog_id, vector<MessageId> mess
     }
   }
   if (!read_content_message_ids.empty()) {
+    // Unread mentions are content-level state, independent from the dialog history read position.
     td_->message_query_manager_->read_message_contents_on_server(dialog_id, std::move(read_content_message_ids), 0,
-                                                                 Auto());
+                                                                 Auto(), false, true);
   }
   if (!read_reaction_message_ids.empty()) {
     td_->message_query_manager_->read_message_reactions_on_server(dialog_id, std::move(read_reaction_message_ids));
@@ -16854,10 +16867,68 @@ Status MessagesManager::view_messages(DialogId dialog_id, vector<MessageId> mess
     send_screenshot_taken_notification_message(d);
   }
 
+  // Shadow Ban already verified the complete unread range in Java. Use an atomic, snapshot-bound
+  // token to set the local count explicitly instead of asking OrderedMessages to infer it from a
+  // history cache that may contain gaps. This branch never calls read_history_on_server.
+  if (!need_read && source == MessageSource::Other) {
+    auto local_read_token = td_->option_manager_->get_option_string("x_moex_shadow_local_read");
+    if (!local_read_token.empty()) {
+      auto token_parts = full_split(Slice(local_read_token), ':');
+      if (token_parts.size() != 4) {
+        td_->option_manager_->set_option_empty("x_moex_shadow_local_read");
+        return Status::Error(400, "Invalid Shadow local read token");
+      }
+      auto r_chat_id = to_integer_safe<int64>(token_parts[0]);
+      auto r_message_id = to_integer_safe<int64>(token_parts[1]);
+      auto r_last_read_inbox_message_id = to_integer_safe<int64>(token_parts[2]);
+      auto r_unread_count = to_integer_safe<int32>(token_parts[3]);
+      if (r_chat_id.is_error() || r_message_id.is_error() || r_last_read_inbox_message_id.is_error() ||
+          r_unread_count.is_error()) {
+        td_->option_manager_->set_option_empty("x_moex_shadow_local_read");
+        return Status::Error(400, "Invalid Shadow local read snapshot");
+      }
+      auto chat_id = get_chat_id_object(dialog_id, "moex shadow local read");
+      bool is_token_request = r_chat_id.ok() == chat_id && message_ids.size() == 1 &&
+                              r_message_id.ok() == message_ids[0].get();
+      if (is_token_request) {
+        td_->option_manager_->set_option_empty("x_moex_shadow_local_read");
+        bool is_supported_dialog = dialog_type == DialogType::User || dialog_type == DialogType::Chat ||
+                                   (dialog_type == DialogType::Channel &&
+                                    !td_->dialog_manager_->is_broadcast_channel(dialog_id) && !d->is_forum);
+        int32 current_unread_count = d->server_unread_count + d->local_unread_count;
+        if (!is_supported_dialog || !max_message_id.is_server() || max_message_id != d->last_message_id ||
+            max_message_id <= d->last_read_inbox_message_id || d->is_marked_as_unread ||
+            d->local_unread_count != 0 || r_message_id.ok() != max_message_id.get() ||
+            r_last_read_inbox_message_id.ok() != d->last_read_inbox_message_id.get() ||
+            r_unread_count.ok() <= 0 || r_unread_count.ok() != current_unread_count) {
+          return Status::Error(400, "Shadow local read snapshot is no longer current");
+        }
+        LOG(INFO) << "Read all-hidden history locally in " << dialog_id << " up to " << max_message_id
+                  << " with " << current_unread_count << " unread messages";
+        read_history_inbox(d, max_message_id, 0, "moex shadow local read");
+        return Status::OK();
+      }
+    }
+  }
+
   if (!need_read) {
     return Status::OK();
   }
 
+  // Ghost mode marks messages as read locally. A later force-read request would normally be
+  // ignored because the local read position no longer advances. Only the matching manual request
+  // may bypass Ghost mode; pending permission must not unlock other chats, topics or saved history.
+  // The existing server-read pipeline may advance to the current local read position.
+  if (force_read && source == MessageSource::DialogHistory && max_message_id.is_valid() &&
+      message_ids.size() == 1 && message_ids[0] == max_message_id &&
+      consume_moex_ghost_read_once(td_, get_chat_id_object(dialog_id, "moex manual read"), max_message_id.get())) {
+    auto server_message_id = max_message_id.get_prev_server_message_id();
+    if (server_message_id.is_valid()) {
+      read_history_on_server(d, server_message_id, true);
+    }
+    return Status::OK();
+  }
+
   if (source == MessageSource::MessageThreadHistory) {
     if (!top_thread_message_id.is_valid() || !max_message_id.is_valid()) {
       return Status::OK();
@@ -16928,6 +16999,26 @@ Status MessagesManager::view_messages(DialogId dialog_id, vector<MessageId> mess
   return Status::OK();
 }
 
+static bool is_moex_ghost_read_blocked(Td *td, DialogId dialog_id) {
+  switch (dialog_id.get_type()) {
+    case DialogType::User:
+    case DialogType::SecretChat:
+      return td->option_manager_->get_option_boolean("x_moex_ghost_read_private");
+    case DialogType::Chat:
+      return td->option_manager_->get_option_boolean("x_moex_ghost_read_groups");
+    case DialogType::Channel:
+      if (td->dialog_manager_->is_broadcast_channel(dialog_id)) {
+        return td->option_manager_->get_option_boolean("x_moex_ghost_read_channels");
+      }
+      return td->option_manager_->get_option_boolean("x_moex_ghost_read_groups");
+    case DialogType::None:
+      return false;
+    default:
+      UNREACHABLE();
+      return false;
+  }
+}
+
 void MessagesManager::read_dialog_inbox(Dialog *d, MessageId max_message_id) {
   if (max_message_id == MessageId()) {
     return;
@@ -16953,13 +17044,14 @@ void MessagesManager::read_dialog_inbox(Dialog *d, MessageId max_message_id) {
     }
   }
 
-  if (read_history_on_server_message_id.is_valid()) {
+  const bool ghost_read = is_moex_ghost_read_blocked(td_, d->dialog_id);
+  if (read_history_on_server_message_id.is_valid() && !ghost_read) {
     // add dummy timeout to not try to repair unread_count in read_history_inbox before server request succeeds
     // the timeout will be overwritten in the read_history_on_server call
     pending_read_history_timeout_.add_timeout_in(d->dialog_id.get(), 0);
   }
   read_history_inbox(d, last_read_message_id, -1, "read_dialog_inbox");
-  if (read_history_on_server_message_id.is_valid()) {
+  if (read_history_on_server_message_id.is_valid() && !ghost_read) {
     // call read_history_on_server after read_history_inbox to not have delay before request if all messages are read
     read_history_on_server(d, read_history_on_server_message_id);
   }
@@ -17650,8 +17742,9 @@ class MessagesManager::ReadMessageThreadHistoryOnServerLogEvent {
   }
 };
 
-void MessagesManager::read_history_on_server(Dialog *d, MessageId max_message_id) {
-  if (td_->auth_manager_->is_bot()) {
+void MessagesManager::read_history_on_server(Dialog *d, MessageId max_message_id, bool allow_in_ghost_mode) {
+  if (td_->auth_manager_->is_bot() ||
+      (!allow_in_ghost_mode && is_moex_ghost_read_blocked(td_, d->dialog_id))) {
     return;
   }
 
@@ -17696,7 +17789,7 @@ void MessagesManager::read_history_on_server(Dialog *d, MessageId max_message_id
 
 void MessagesManager::read_message_thread_history_on_server(Dialog *d, MessageId top_thread_message_id,
                                                             MessageId max_message_id, MessageId last_message_id) {
-  if (td_->auth_manager_->is_bot()) {
+  if (td_->auth_manager_->is_bot() || is_moex_ghost_read_blocked(td_, d->dialog_id)) {
     return;
   }
 
diff --git a/td/telegram/MessagesManager.h b/td/telegram/MessagesManager.h
index 8eaf7c9..a8797b1 100644
--- a/td/telegram/MessagesManager.h
+++ b/td/telegram/MessagesManager.h
@@ -2151,7 +2151,7 @@ class MessagesManager final : public Actor {
 
   void repair_dialog_unread_poll_vote_count(Dialog *d, Promise<Unit> &&promise, const char *source);
 
-  void read_history_on_server(Dialog *d, MessageId max_message_id);
+  void read_history_on_server(Dialog *d, MessageId max_message_id, bool allow_in_ghost_mode = false);
 
   void do_read_history_on_server(DialogId dialog_id);
 
diff --git a/td/telegram/OnlineManager.cpp b/td/telegram/OnlineManager.cpp
index e73f6cc..8432d11 100644
--- a/td/telegram/OnlineManager.cpp
+++ b/td/telegram/OnlineManager.cpp
@@ -126,6 +126,9 @@ bool OnlineManager::is_online() const {
 }
 
 void OnlineManager::set_is_online(bool is_online) {
+  if (td_->option_manager_->get_option_boolean("x_moex_ghost_online")) {
+    is_online = false;
+  }
   if (is_online == is_online_) {
     return;
   }
diff --git a/td/telegram/Requests.cpp b/td/telegram/Requests.cpp
index faf6823..85b282b 100644
--- a/td/telegram/Requests.cpp
+++ b/td/telegram/Requests.cpp
@@ -4860,6 +4860,9 @@ void Requests::on_request(uint64 id, const td_api::deleteChatReplyMarkup &reques
 
 void Requests::on_request(uint64 id, td_api::sendChatAction &request) {
   CREATE_OK_REQUEST_PROMISE();
+  if (td_->option_manager_->get_option_boolean("x_moex_ghost_actions")) {
+    return promise.set_value(Unit());
+  }
   DialogId dialog_id(request.chat_id_);
   TRY_RESULT_PROMISE(promise, message_topic, MessageTopic::get_message_topic(td_, dialog_id, request.topic_id_));
   td_->dialog_action_manager_->send_dialog_action(dialog_id, message_topic,
diff --git a/td/telegram/SavedMessagesManager.cpp b/td/telegram/SavedMessagesManager.cpp
index 3e3e3e7..6a07dbd 100644
--- a/td/telegram/SavedMessagesManager.cpp
+++ b/td/telegram/SavedMessagesManager.cpp
@@ -1099,7 +1099,7 @@ void SavedMessagesManager::read_monoforum_topic_messages(DialogId dialog_id,
 
   read_topic_messages(topic, read_inbox_max_message_id, -1);
 
-  if (topic->is_changed_) {
+  if (topic->is_changed_ && !td_->option_manager_->get_option_boolean("x_moex_ghost_read_private")) {
     td_->create_handler<ReadSavedHistoryQuery>()->send(dialog_id, saved_messages_topic_id,
                                                        read_inbox_max_message_id.get_prev_server_message_id());
   }
